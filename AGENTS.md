# 최상위 컨텍스트: Follow Me! 시스템

## 1. 시스템 요약
"Follow Me!" 시스템은 복잡한 웹사이트에서 사용자가 길을 잃지 않도록 단계별 가이드를 제공하여 사용자 경험을 개선하는 것을 목표로 합니다. 브라우저 확장 프로그램을 통해 사용자의 행동 데이터를 수집하고, 백엔드 분석 모듈에서 이 데이터를 처리하여 성공적인 경로를 식별한 후, 이 경로를 다른 사용자에게 실시간 가이드로 제공합니다. 시스템은 **확장 프로그램(데이터 수집)**, **서버(데이터 관리)**, **분석(경로 생성)**의 세 가지 핵심 에이전트로 구성됩니다.

## 2. 에이전트 목록 및 역할
| 에이전트 (컨텍스트 파일) | 경로 | 핵심 역할 및 기능 | 주요 입/출력 |
|---|---|---|---|
| **확장 프로그램 에이전트** | `/extension` | 사용자의 웹 활동(클릭, 입력 등)을 감지하고 수집합니다. 분석된 가이드를 받아 사용자 화면에 시각적으로 표시합니다. | **입력**: 사용자 행동, 서버 API <br> **출력**: 로그 데이터, DOM 조작 |
| **서버 에이전트** | `/server` | 확장 프로그램으로부터 로그 데이터를 받아 데이터베이스에 저장하고, 요청 시 분석된 가이드를 제공하는 중앙 API 서버입니다. | **입력**: 확장 프로그램의 API 요청 <br> **출력**: 데이터베이스 읽기/쓰기 |
| **분석 에이전트** | `/analysis` | 데이터베이스에 저장된 원시 로그를 읽어, 성공적인 사용자 경로를 찾는 배치(batch) 분석을 수행하고, 결과를 가이드 데이터로 만들어 다시 데이터베이스에 저장합니다. | **입력**: 데이터베이스의 원시 로그 <br> **출력**: 데이터베이스의 분석된 가이드 |

## 3. 아키텍처 개요 및 데이터 흐름
본 시스템은 클라이언트-서버 아키텍처와 오프라인 배치 분석 모듈이 결합된 구조입니다.

1.  **데이터 수집 (확장 프로그램)**: 사용자가 웹사이트에서 활동하면, `content.js`가 이벤트를 감지하여 `background.js`로 전송합니다.
2.  **데이터 전송 (확장 프로그램 → 서버)**: `background.js`는 수집된 로그를 일정 주기로 버퍼링하여 `서버`의 `/api/v1/log_batch` 엔드포인트로 전송합니다.
3.  **데이터 저장 (서버 → DB)**: `서버`는 수신한 로그를 MongoDB의 `event_logs` 컬렉션에 저장합니다.
4.  **경로 분석 (분석 → DB)**: 별도의 `분석` 모듈(`main_analyzer.py`)이 실행되면, `event_logs`에서 데이터를 읽어 가장 빈도가 높은 성공 경로를 찾습니다.
5.  **가이드 생성 (분석 → DB)**: 분석된 경로는 가이드 형식으로 가공되어 MongoDB의 `analyzed_guides` 컬렉션에 저장됩니다.
6.  **가이드 요청 (확장 프로그램 → 서버)**: 사용자가 웹페이지에 접속하면, `content.js`는 현재 URL에 대한 가이드를 `서버`의 `/api/v1/guide` 엔드포인트에 요청합니다.
7.  **가이드 표시 (서버 → 확장 프로그램)**: `서버`는 `analyzed_guides`에서 해당 URL의 가이드를 찾아 `확장 프로그램`에 전달하고, `content.js`의 `GuideManager`가 이를 화면에 표시합니다.

## 4. 현재 상태 및 개선 권장 사항
- **프로토타입 완성**: 현재 시스템은 데이터 수집부터 분석, 가이드 표시에 이르는 핵심 데이터 흐름을 모두 구현한 기능적 프로토타입 단계입니다.
- **주요 기술 부채**:
    - **분석 알고리즘**: 현재 경로 분석은 단순 빈도 계산에 의존하므로, 더 정교한 시퀀스 마이닝 알고리즘으로 교체가 필요합니다.
    - **설정 관리**: API 서버 주소나 분석 목표 URL 등이 코드에 하드코딩되어 있어, 환경별 설정 분리가 시급합니다.
    - **모듈 간 결합**: `analysis` 모듈이 `server`의 `.env` 파일을 직접 참조하는 등 모듈 간 결합도가 높아 독립적인 운영과 테스트에 제약이 있습니다.
- **다음 단계 추천**:
    - **기능 개선**: 관리자가 분석할 목표 페이지를 동적으로 설정하는 기능 추가.
    - **구조 개선**: 각 모듈이 독립적으로 설정을 가질 수 있도록 의존성을 제거하고, API URL 등을 중앙에서 관리하도록 리팩터링.
    - **테스트 보완**: 각 모듈의 핵심 로직에 대한 단위 테스트를 작성하고, 전체 데이터 흐름을 검증하는 통합 테스트를 구축.
